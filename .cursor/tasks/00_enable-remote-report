<task>
実装して下さい
</task>

---


(ここから書いてね)

- JSON Schemaから対応するRubyのコードを生成するライブラリを実装した
  - JSON Schema文字列またはファイルからData.defineクラスを生成する機能を実装
  - 次の機能を実装した
    - 型のバリデーション (string, integer, number, boolean, array, object)
    - オプションパラメータと必須パラメータの対応
    - 数値の制約 (minimum, maximum)
    - 文字列の制約 (minLength, maxLength, pattern)
    - 配列の制約 (minItems, maxItems)
  - RSpecによるテストを実装して機能を確認
  - CLIツールとして実行可能にした
    - `-o` オプションで出力ファイルを指定可能
    - `-c` オプションでクラス名を指定可能
  - 使用方法のREADMEを作成した
- 実際にテスト用のスキーマファイルからRubyコードを生成して動作確認を行った
  - 正しくData.defineクラスが生成されることを確認
  - 型と制約のバリデーションが正しく実装されていることを確認

【やり残し・改善点】
- 複雑なスキーマでのテストが不十分
- バリデーションメッセージがすべて英語（日本語メッセージ対応）
- formatプロパティの対応が不完全（emailなどの形式チェック未実装）
- エラーハンドリングの強化が必要
- 生成されるコードの品質とフォーマットの改善

